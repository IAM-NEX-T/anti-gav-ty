server:
  host: "0.0.0.0"
  port: 8080
  read_timeout: 10s
  write_timeout: 10s
  idle_timeout: 60s

log:
  level: "info"
  format: "json"

database:
  url: "postgres://lamo:lamo@localhost:5432/lamoseve?sslmode=disable"
  max_open_conns: 25
  max_idle_conns: 5
  conn_max_lifetime: 5m

migrations:
  path: "migrations"

nextdns:
  api_key: ""
  profile_id: ""
